/*
step1: fill the array with the data given by the user
step2: to swap the values in the array we need a container
step3: we loop inside the arry from the first to midd value inside the array and we step by one each time 
step4: we put the first value of the array in the container 
step5: 
step6: the last value  takes the container value 
step7: we return the reversed array

 */


// console.log(2==="2")
// if (true)
    // if ({})
    // if ([])
    // if (42)
    // if ("0")
    // if ("false")
    // if (new Date())
    // if (-42)
    // if (12n)
    // if (3.14)
    // if (-3.14)
    // if (Infinity)
    
        // console.log(if (-5))
    